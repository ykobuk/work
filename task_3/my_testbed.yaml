devices:
  vm:
    os: 'linux'
    tacacs:
      username: write_username
    passwords:
      linux: write_password
    connections:
      linux:
        protocol: ssh
        ip: write IP 
    type: 'linux'

testbed:
  servers:
    server_alias:
      server: localhost
      address: write IP
      username: write_username
      password: write_password

